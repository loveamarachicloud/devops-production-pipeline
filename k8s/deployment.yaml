apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-pipeline-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: devops-pipeline-app
  template:
    metadata:
      labels:
        app: devops-pipeline-app
    spec:
      containers:
        - name: devops-pipeline-app
          image: loveamarachicloud/devops-pipeline-app:latest
          ports:
            - containerPort: 80